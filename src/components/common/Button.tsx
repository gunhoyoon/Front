import React from "react";

interface Props {
  onCilck?: Function;
  type?: "button" | "submit";
  style?: object;
  // text? :
}

const Button = () => {
  return <div>Button</div>;
};

export default Button;
